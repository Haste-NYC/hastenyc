---
phase: 01-static-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
  - src/pages/Index.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to all sections (Overview, Features, About, FAQ, Pricing) via fixed header navigation"
    - "User can click 'Start Trial' CTA and page scrolls smoothly to pricing section"
    - "User on mobile device sees hamburger menu that opens a drawer with navigation links"
    - "User clicks a nav link in mobile menu and menu closes after scrolling"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Fixed header with desktop nav, mobile Sheet menu, smooth scroll handler"
      min_lines: 80
      exports: ["default"]
    - path: "src/pages/Index.tsx"
      provides: "One-page layout with section IDs and wrapper structure"
      min_lines: 40
      exports: ["default"]
  key_links:
    - from: "src/components/Header.tsx"
      to: "section IDs in Index.tsx"
      via: "scrollToSection function with offset calculation"
      pattern: "getElementById.*hero|features|about|faq|pricing"
    - from: "src/components/Header.tsx"
      to: "@/components/ui/sheet"
      via: "Sheet, SheetTrigger, SheetContent imports"
      pattern: "import.*Sheet.*from.*@/components/ui/sheet"
---

<objective>
Transform the header into a fixed navigation bar with section links and smooth scroll, and restructure Index.tsx into a one-page layout with section IDs.

Purpose: Establish the one-page scroll architecture that all other sections will integrate into. This creates the navigation foundation for the premium landing page experience.

Output:
- Updated Header.tsx with desktop nav links, "Start Trial" CTA, "Sign In" link, and mobile hamburger menu
- Updated Index.tsx with section-based layout (hero, features, about, faq, pricing)
- Working smooth scroll with header offset compensation
</objective>

<execution_context>
@/Users/haste/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/haste/Desktop/hastenyc/.planning/PROJECT.md
@/Users/haste/Desktop/hastenyc/.planning/ROADMAP.md
@/Users/haste/Desktop/hastenyc/.planning/STATE.md
@/Users/haste/Desktop/hastenyc/.planning/phases/01-static-foundation/01-RESEARCH.md

# Existing components (read for context)
@/Users/haste/Desktop/hastenyc/src/components/Header.tsx
@/Users/haste/Desktop/hastenyc/src/pages/Index.tsx
@/Users/haste/Desktop/hastenyc/src/components/ui/sheet.tsx
@/Users/haste/Desktop/hastenyc/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Update Header with navigation and mobile menu</name>
  <files>src/components/Header.tsx</files>
  <action>
    Transform the existing minimal Header into a fixed navigation bar with:

    1. **Navigation structure:**
       - navLinks array: Overview (#hero), Features (#features), About (#about), FAQ (#faq)
       - Desktop nav: horizontal links with uppercase tracking-wider text styling
       - "Start Trial" button using existing Button variant="hero" that scrolls to #pricing
       - "Sign In" text link to /signin route

    2. **Mobile hamburger menu:**
       - Import Sheet, SheetTrigger, SheetContent from @/components/ui/sheet
       - Import Menu icon from lucide-react
       - Add mobile menu state: `const [mobileMenuOpen, setMobileMenuOpen] = useState(false)`
       - SheetTrigger with Menu icon (visible on mobile via md:hidden class)
       - SheetContent side="right" width 300px with vertical navigation links
       - Include "Start Trial" button and "Sign In" link in mobile menu

    3. **Smooth scroll handler:**
       - Create `scrollToSection(id: string)` function that:
         - Gets element by ID
         - Calculates header offset (80px)
         - Uses window.scrollTo with smooth behavior
         - Closes mobile menu via setMobileMenuOpen(false)
       - Attach to all nav link onClick handlers with preventDefault

    4. **Styling:**
       - Fixed header: `fixed top-0 w-full z-50 py-4 px-6 bg-background/95 backdrop-blur-sm border-b border-border/40`
       - Desktop nav hidden on mobile: `hidden md:flex`
       - Mobile menu trigger shown only on mobile: `md:hidden`
       - Keep existing HasteLogo component

    **Reference:** Use code example from 01-RESEARCH.md "Example 1: Fixed Header with Navigation" as template. Adapt existing Header structure, don't replace entirely.
  </action>
  <verify>
    1. Run `npm run dev` and open localhost:5173
    2. Check desktop header shows: Logo | Overview Features About FAQ | Start Trial | Sign In
    3. Resize to mobile (< 768px), verify hamburger menu appears
    4. Click hamburger, verify Sheet drawer opens from right
    5. Click a nav link, verify smooth scroll and menu closes
    6. Click "Start Trial", verify scrolls to bottom (pricing section placeholder)
  </verify>
  <done>
    Header.tsx exports a fixed navigation bar with desktop horizontal nav, mobile Sheet menu, smooth scroll handler with offset, and all links properly wired to section IDs.
  </done>
</task>

<task type="auto">
  <name>Restructure Index.tsx for one-page layout</name>
  <files>src/pages/Index.tsx</files>
  <action>
    Restructure Index.tsx from multi-section component stack to one-page scroll layout:

    1. **Update main structure:**
       - Replace existing `<main>` content with section-based layout
       - Each section gets unique ID attribute: hero, features, about, faq, pricing
       - Add pt-20 (80px padding-top) to first section to account for fixed header

    2. **Section layout:**
       ```tsx
       <main>
         <section id="hero" className="pt-20">
           <HeroSection />
         </section>

         <section id="features" className="min-h-screen py-24">
           <FeatureSection />
         </section>

         <section id="about" className="py-24">
           {/* Placeholder div for now - AboutSection created in Plan 03 */}
           <div className="min-h-[400px] flex items-center justify-center">
             <p className="text-foreground/60">About Section (Plan 03)</p>
           </div>
         </section>

         <section id="faq" className="py-24">
           {/* Placeholder div for now - FAQSection created in Plan 03 */}
           <div className="min-h-[400px] flex items-center justify-center">
             <p className="text-foreground/60">FAQ Section (Plan 03)</p>
           </div>
         </section>

         <section id="pricing" className="py-24">
           {/* Placeholder div for now - PricingSection created in Plan 03 */}
           <div className="min-h-[400px] flex items-center justify-center">
             <p className="text-foreground/60">Pricing Section (Plan 03)</p>
           </div>
         </section>
       </main>
       ```

    3. **Remove components temporarily:**
       - Remove VideoSection (not in Phase 1 requirements)
       - Remove CTASection (replaced by pricing section)
       - Keep Header, Footer, SEO

    4. **CSS scroll behavior:**
       - Verify src/index.css has `scroll-behavior: smooth` (research confirms it exists)
       - If missing, add to html selector

    **Why placeholders:** Plans 01-02 and 01-03 run in parallel. This plan creates the structure; Plan 03 fills in About, FAQ, Pricing sections.
  </action>
  <verify>
    1. Run `npm run dev`
    2. Verify page loads with hero and features visible
    3. Verify 3 placeholder sections render with gray text
    4. Click header nav links, verify smooth scroll to each section ID
    5. Check no console errors about missing imports
  </verify>
  <done>
    Index.tsx structured as one-page layout with 5 section IDs (hero, features, about, faq, pricing), proper spacing/padding, and placeholder divs for sections not yet implemented.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Navigation functionality:**
   - Desktop header displays all nav links horizontally
   - Mobile header shows hamburger menu that opens Sheet drawer
   - All nav links scroll smoothly to correct section with proper offset (no content hidden under header)
   - Mobile menu closes after clicking a link

2. **Page structure:**
   - Index.tsx has 5 sections with IDs matching header nav links
   - Hero and Features sections render with existing components
   - About, FAQ, Pricing sections show placeholder text (filled in by Plan 03)
   - No horizontal scroll on mobile
   - Fixed header stays at top while scrolling

3. **Technical checks:**
   - `npm run build` succeeds with no TypeScript errors
   - `npm run lint` passes with no violations
   - No console warnings in browser
</verification>

<success_criteria>
Navigation architecture complete when:
- User can click any nav link (desktop or mobile) and page scrolls smoothly to that section
- Header remains fixed at top, content scrolls beneath it
- Mobile menu opens/closes correctly and includes all navigation options
- One-page structure established with section IDs for all required sections
- Hero and Features sections render existing components, other sections show placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-foundation/01-01-SUMMARY.md` documenting navigation implementation, scroll behavior, mobile menu functionality, and any decisions made during implementation.
</output>
