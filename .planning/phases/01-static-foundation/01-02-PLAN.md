---
phase: 01-static-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HeroSection.tsx
  - src/components/FeatureSection.tsx
autonomous: true

must_haves:
  truths:
    - "User sees ethereal gradient background in hero section (soft, glowing, x.ai-style)"
    - "User sees bold headline with clear value proposition in hero section"
    - "User sees product screenshot in hero section"
    - "User sees 4 distinct feature categories with bold headlines"
    - "User sees big bold statistics (300X faster, 100% accurate, TPN+ compliant)"
    - "User sees full-height feature sections with generous negative space"
  artifacts:
    - path: "src/components/HeroSection.tsx"
      provides: "Hero with ethereal gradient, headline, screenshot, CTA"
      min_lines: 80
      exports: ["default"]
    - path: "src/components/FeatureSection.tsx"
      provides: "4 feature categories with statistics and full-height sections"
      min_lines: 150
      exports: ["default"]
  key_links:
    - from: "src/components/HeroSection.tsx"
      to: "Ethereal gradient pattern"
      via: "Inline style with radial-gradient matching VideoSection technique"
      pattern: "radial-gradient.*rgba"
    - from: "src/components/FeatureSection.tsx"
      to: "Feature category content"
      via: "Array of 4 feature objects with title, description, statistic"
      pattern: "features.*=.*\\["
---

<objective>
Transform the hero and features sections into premium, full-height showcases with ethereal gradients, bold typography, statistics, and generous spacing.

Purpose: Create the visual centerpiece of the landing page—the hero section that hooks visitors and the feature sections that demonstrate value. These sections establish the premium aesthetic and conversion-focused messaging.

Output:
- Updated HeroSection.tsx with ethereal gradient background and revised content
- Updated FeatureSection.tsx with 4 feature categories, bold statistics, and full-height layout
</objective>

<execution_context>
@/Users/haste/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/haste/Desktop/hastenyc/.planning/PROJECT.md
@/Users/haste/Desktop/hastenyc/.planning/ROADMAP.md
@/Users/haste/Desktop/hastenyc/.planning/phases/01-static-foundation/01-RESEARCH.md

# Existing components for reference
@/Users/haste/Desktop/hastenyc/src/components/HeroSection.tsx
@/Users/haste/Desktop/hastenyc/src/components/FeatureSection.tsx
@/Users/haste/Desktop/hastenyc/src/components/VideoSection.tsx
@/Users/haste/Desktop/hastenyc/src/assets/conform-studio-logo.png
</context>

<tasks>

<task type="auto">
  <name>Add ethereal gradient to HeroSection</name>
  <files>src/components/HeroSection.tsx</files>
  <action>
    Enhance HeroSection with ethereal gradient background and updated content:

    1. **Ethereal gradient background:**
       - Add gradient div before existing content (z-0)
       - Use VideoSection pattern as reference (research Example 3)
       - Gradient positioning: absolute, pointer-events-none, top 40%, left 50%, translate(-50%, -50%)
       - Size: width 200vw, height 200vh (extends beyond viewport)
       - Color: `radial-gradient(ellipse 50% 50% at 50% 50%, rgba(200, 100, 255, 0.3) 0%, rgba(150, 100, 255, 0.15) 20%, rgba(100, 150, 255, 0.08) 35%, transparent 55%)`
       - Multi-color soft purple/blue blend for premium feel

    2. **Content structure updates:**
       - Wrap section with relative positioning
       - Add min-h-[calc(100vh-80px)] to section for full viewport height minus header
       - Ensure content div has z-10 relative positioning (above gradient)
       - Keep existing Framer Motion animations

    3. **Content revisions:**
       - Update headline to be bolder: Change tagline to emphasize "300X Faster" or "Instant Timeline Conform"
       - Keep existing Conform Studio logo image
       - Update description to focus on core value prop: "Premiere to Resolve timeline conversion in seconds, not days"
       - Change CTA from "Schedule a Demo" to "Start Free Trial" (scroll to pricing)
       - Add secondary CTA: "Watch Demo" link below primary button

    4. **Product screenshot:**
       - Below CTA buttons, add product screenshot container
       - Use existing asset (check /src/assets/ for product screenshots)
       - If no screenshot exists, use placeholder with aspect ratio 16:9
       - Wrap in motion.div with whileInView animation (once: true)
       - Max width ~800px, rounded corners, subtle shadow

    **Styling approach:** Don't change drastically—enhance existing dark minimal aesthetic. Keep uppercase tracking, keep Framer Motion animations, add gradient layer behind.
  </action>
  <verify>
    1. Run `npm run dev` and navigate to hero section
    2. Verify ethereal gradient visible (soft purple/blue glow)
    3. Verify gradient doesn't interfere with text readability
    4. Verify "Start Free Trial" button scrolls to pricing section (implemented in Plan 01)
    5. Verify product screenshot renders (or placeholder if no asset)
    6. Check mobile responsiveness (gradient scales properly, content readable)
  </verify>
  <done>
    HeroSection.tsx has ethereal gradient background matching x.ai aesthetic, updated value proposition emphasizing speed/accuracy, "Start Free Trial" CTA that scrolls to pricing, and product screenshot below CTA.
  </done>
</task>

<task type="auto">
  <name>Restructure FeatureSection for 4 categories with statistics</name>
  <files>src/components/FeatureSection.tsx</files>
  <action>
    Transform FeatureSection into 4 full-height feature showcases with bold statistics:

    1. **4 Priority Feature Categories (from PROJECT.md and RESEARCH.md):**

       **Category 1: Core Conversion**
       - Headline: "Premiere to Resolve, Instantly"
       - Statistic: "300X Faster" (large, bold, gradient text)
       - Description: "What once took days now takes seconds. Migrate timelines from Adobe Premiere to DaVinci Resolve with AI-powered automation that preserves every edit, every effect, every frame."
       - UI screenshot placeholder: [To be added - multi-format timeline conversion visual]

       **Category 2: Timeline Preservation**
       - Headline: "100% Accurate, Every Time"
       - Statistic: "100% Frame Accuracy" (large, bold, gradient text)
       - Description: "Multi-track timelines, complex effects, speed ramps, bezier curves—all preserved perfectly. No manual fixes, no frame offsets, no lost work."
       - UI screenshot placeholder: [To be added - timeline comparison visual]

       **Category 3: Advanced Effects**
       - Headline: "Complex Effects, Zero Compromises"
       - Statistic: "Unlimited Projects" (large, bold)
       - Description: "Speed ramps, motion keyframes, Essential Graphics, transitions—Haste Conform Studio handles the effects that break other tools. Built for professional film and TV workflows."
       - UI screenshot placeholder: [To be added - effects preservation visual]

       **Category 4: Quality Control**
       - Headline: "TPN+ Compliant, Studio Ready"
       - Statistic: "TPN+ Certified" (large, bold, badge-style)
       - Description: "Your media never leaves your pipeline. Built for high-security post-production environments trusted by studios and networks. SOC2 compliant, on-premise deployment available."
       - UI screenshot placeholder: [To be added - security/compliance visual]

    2. **Component structure:**
       - Each feature = full section with min-h-screen
       - Alternate layout: odd features (1, 3) have text left/image right, even features (2, 4) have image left/text right
       - Use CSS Grid for layout (grid-cols-1 md:grid-cols-2, gap-12)
       - Generous padding: py-24 on each section
       - Max width container: max-w-7xl mx-auto

    3. **Statistic styling:**
       - Large text: text-6xl md:text-8xl font-bold
       - Gradient for "300X" and "100%": bg-gradient-to-r from-pink-500 to-purple-500 bg-clip-text text-transparent
       - Position above headline or as eyebrow element

    4. **Image placeholders:**
       - For each feature, create placeholder div: aspect-[16/9] bg-gray-800 rounded-xl
       - Add "UI Screenshot" text in center (text-gray-600)
       - Comment in code where actual screenshot should go
       - Use existing product images from /src/assets if available

    5. **Framer Motion animations:**
       - Wrap each feature section in motion.div
       - Use whileInView with viewport once: true, margin: "-100px"
       - Stagger animations: text first, then image (delay: 0.2s)

    **Layout reference:** Think frame.io feature sections—large stats, bold headlines, generous whitespace, alternating image/text sides.
  </action>
  <verify>
    1. Run `npm run dev` and scroll to features section
    2. Verify 4 distinct feature sections render
    3. Verify statistics appear large and bold (300X, 100%, etc.)
    4. Verify alternating layout (text left/right on desktop)
    5. Verify full-height sections with generous spacing
    6. Check mobile: sections stack vertically, text remains readable
    7. Verify Framer Motion animations trigger once as sections scroll into view
  </verify>
  <done>
    FeatureSection.tsx displays 4 full-height feature categories (Core Conversion, Timeline Preservation, Advanced Effects, Quality Control) with large bold statistics, alternating layout, generous negative space, and placeholder image containers for screenshots.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Hero section:**
   - Ethereal gradient background visible (soft purple/blue glow)
   - Bold value proposition emphasizing speed and accuracy
   - "Start Free Trial" CTA scrolls to pricing section
   - Product screenshot or placeholder renders below CTA
   - Content readable on all viewport sizes

2. **Features section:**
   - 4 distinct full-height sections render
   - Bold statistics prominent: "300X Faster", "100% Accurate", "TPN+ Certified"
   - Text and image placeholders alternate sides (desktop)
   - Generous whitespace between sections
   - Mobile responsive: sections stack vertically

3. **Technical checks:**
   - `npm run build` succeeds with no TypeScript errors
   - `npm run lint` passes
   - No console warnings in browser
   - Framer Motion animations perform smoothly
</verification>

<success_criteria>
Hero and Features sections complete when:
- User sees ethereal gradient in hero matching x.ai premium aesthetic
- User sees clear value proposition and "Start Free Trial" CTA in hero
- User sees 4 feature categories with big bold statistics
- User sees full-height sections with generous negative space
- All sections responsive on mobile devices
- Animations trigger once as user scrolls
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-foundation/01-02-SUMMARY.md` documenting gradient implementation, feature category content, statistic styling, layout decisions, and placeholder locations for product screenshots.
</output>
