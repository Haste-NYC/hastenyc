---
phase: 01-static-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/AboutSection.tsx
  - src/components/FAQSection.tsx
  - src/components/PricingSection.tsx
  - src/pages/Index.tsx
autonomous: true

must_haves:
  truths:
    - "User sees Haste NYC company story and trust signals in About section"
    - "User sees FAQ accordion that expands/collapses questions"
    - "User sees three pricing tiers (Freelancer, Studio, Enterprise)"
    - "User can toggle between monthly and yearly pricing"
    - "User sees feature lists for each pricing tier"
  artifacts:
    - path: "src/components/AboutSection.tsx"
      provides: "Company story, trust signals, dark themed section"
      min_lines: 60
      exports: ["default"]
    - path: "src/components/FAQSection.tsx"
      provides: "Accordion with common questions, dark themed"
      min_lines: 80
      exports: ["default"]
    - path: "src/components/PricingSection.tsx"
      provides: "Wrapper component using existing PricingPlans"
      min_lines: 30
      exports: ["default"]
    - path: "src/pages/Index.tsx"
      provides: "Updated imports and section rendering"
      exports: ["default"]
  key_links:
    - from: "src/components/FAQSection.tsx"
      to: "@/components/ui/accordion"
      via: "Accordion, AccordionContent, AccordionItem, AccordionTrigger imports"
      pattern: "import.*Accordion.*from.*@/components/ui/accordion"
    - from: "src/components/PricingSection.tsx"
      to: "@/components/PricingPlans"
      via: "PricingPlans component import and render"
      pattern: "import.*PricingPlans.*from"
    - from: "src/pages/Index.tsx"
      to: "New section components"
      via: "Import and render AboutSection, FAQSection, PricingSection"
      pattern: "import.*AboutSection|FAQSection|PricingSection"
---

<objective>
Create the About, FAQ, and Pricing sections to complete the one-page landing page content.

Purpose: Provide the trust-building content (About), answer common objections (FAQ), and present the conversion point (Pricing) that turns engaged visitors into trial signups.

Output:
- New AboutSection.tsx with Haste NYC story and trust signals
- New FAQSection.tsx with accordion of common questions
- New PricingSection.tsx wrapper using existing PricingPlans component
- Updated Index.tsx with sections replacing placeholders from Plan 01
</objective>

<execution_context>
@/Users/haste/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/haste/Desktop/hastenyc/.planning/PROJECT.md
@/Users/haste/Desktop/hastenyc/.planning/ROADMAP.md
@/Users/haste/Desktop/hastenyc/.planning/phases/01-static-foundation/01-RESEARCH.md

# Existing components for reference
@/Users/haste/Desktop/hastenyc/src/components/PricingPlans.tsx
@/Users/haste/Desktop/hastenyc/src/components/ui/accordion.tsx
@/Users/haste/Desktop/hastenyc/src/pages/Index.tsx
</context>

<tasks>

<task type="auto">
  <name>Create AboutSection component</name>
  <files>src/components/AboutSection.tsx</files>
  <action>
    Create AboutSection.tsx with company story and trust signals:

    1. **Component structure:**
       - Export default functional component
       - Max width container: max-w-4xl mx-auto
       - Padding: py-24 px-6
       - Dark minimal styling matching existing components

    2. **Content sections:**

       **Section title:**
       - "About Haste NYC" headline (text-3xl md:text-5xl, uppercase, tracking-wide)
       - Centered, with Framer Motion fade-in animation

       **Company story (2-3 paragraphs):**
       - "Haste NYC builds AI-powered automation tools for post-production professionals. Founded by editors who've felt the pain of manual conform workflows, we're on a mission to eliminate the tedious parts of post and free creative professionals to do what they do best: create."
       - "Conform Studio is our first product—a solution to the timeline migration problem that's plagued the industry for decades. What started as an internal tool for high-volume conform projects is now available to studios and freelancers worldwide."
       - Text styling: text-foreground/80, text-sm md:text-base, leading-relaxed, uppercase tracking-[0.04em]

       **Trust signals:**
       - Subheading: "Trusted By" (text-xl, uppercase, mb-8)
       - Bullet list or grid of trust indicators:
         - "TPN+ Certified for studio security"
         - "Used by post-production facilities in LA, NYC, London"
         - "SOC2 Type II compliant"
         - "99.9% uptime SLA for Enterprise customers"
       - Style as simple text list or badge-style elements (avoid logos unless assets exist)

    3. **Framer Motion animations:**
       - Wrap title in motion.h2 with initial opacity 0, whileInView opacity 1, viewport once: true
       - Wrap paragraphs in motion.div with staggered delays (0.1s, 0.2s)

    4. **Styling notes:**
       - Match existing dark theme (bg-background, text-foreground)
       - Use existing Tailwind classes
       - Keep uppercase tracking consistent with rest of site
       - Generous line height for readability

    **Content source:** Use PROJECT.md context for company positioning. Keep it concise—this is a conversion-focused landing page, not an About Us deep dive.
  </action>
  <verify>
    1. Run `npm run dev` and scroll to About section
    2. Verify company story renders with 2-3 paragraphs
    3. Verify trust signals list displays
    4. Verify dark theme styling matches rest of site
    5. Verify Framer Motion animations trigger once
    6. Check mobile: text readable, no horizontal scroll
  </verify>
  <done>
    AboutSection.tsx exports a dark-themed section with Haste NYC company story (2-3 paragraphs), trust signals (TPN+, SOC2, uptime SLA), Framer Motion animations, and styling consistent with existing components.
  </done>
</task>

<task type="auto">
  <name>Create FAQSection component</name>
  <files>src/components/FAQSection.tsx</files>
  <action>
    Create FAQSection.tsx with accordion of common questions:

    1. **Component structure:**
       - Import Accordion, AccordionContent, AccordionItem, AccordionTrigger from @/components/ui/accordion
       - Import motion from framer-motion
       - Export default functional component
       - Container: max-w-3xl mx-auto, py-24 px-6

    2. **FAQ content array (6-8 questions):**

       ```typescript
       const faqs = [
         {
           question: "How fast is the conform process?",
           answer: "Haste Conform Studio completes complex conform tasks at 300X the speed of human teams. What once took days now takes seconds, without sacrificing accuracy."
         },
         {
           question: "Is my media secure?",
           answer: "Your media never leaves your pipeline. Our platform is privacy-forward, built for TPN-compliant environments, and trusted by industry leaders."
         },
         {
           question: "What formats do you support?",
           answer: "We support migration between Adobe Premiere, DaVinci Resolve, Avid Media Composer, and Pro Tools. More integrations are coming soon."
         },
         {
           question: "Do you offer a free trial?",
           answer: "Yes! We offer a 7-day free trial for all new users. No credit card required to start."
         },
         {
           question: "What happens to my existing projects?",
           answer: "All your project files remain exactly where they are. Haste Conform Studio creates new Resolve timelines while preserving your original Premiere projects."
         },
         {
           question: "Can I cancel anytime?",
           answer: "Yes, you can cancel your subscription at any time. No long-term contracts or cancellation fees."
         },
         {
           question: "What's included in Enterprise?",
           answer: "Enterprise includes unlimited seats, dedicated support, custom integrations, on-premise deployment options, and SLA guarantees. Contact us for a custom quote."
         },
       ];
       ```

    3. **Accordion implementation:**
       - Use Shadcn Accordion component with type="single" collapsible
       - Map over faqs array to create AccordionItem elements
       - Each item: value={`item-${index}`}, className with border and rounded corners
       - AccordionTrigger: text-left, text-base md:text-lg, font-medium, hover:no-underline
       - AccordionContent: text-foreground/80, text-sm md:text-base, leading-relaxed

    4. **Section styling:**
       - Title: "Frequently Asked Questions" (text-3xl md:text-5xl, uppercase, centered, mb-12)
       - Wrap title in motion.h2 with fade-in animation
       - Accordion spacing: space-y-4 between items
       - Dark theme: border-border/40, bg-card/30 on items

    **Reference:** Use code example from 01-RESEARCH.md "Example 2: FAQ Section with Accordion" as template.
  </action>
  <verify>
    1. Run `npm run dev` and scroll to FAQ section
    2. Verify "Frequently Asked Questions" title renders
    3. Verify accordion items visible (collapsed by default)
    4. Click an accordion item, verify it expands smoothly
    5. Click another item, verify first collapses (single mode)
    6. Check styling: dark theme, rounded corners, proper spacing
    7. Test mobile: accordion works, text readable
  </verify>
  <done>
    FAQSection.tsx exports a dark-themed section with "Frequently Asked Questions" title, Shadcn Accordion component with 6-8 common questions, single-item expansion mode, and styling consistent with site aesthetic.
  </done>
</task>

<task type="auto">
  <name>Create PricingSection wrapper and update Index.tsx</name>
  <files>src/components/PricingSection.tsx, src/pages/Index.tsx</files>
  <action>
    Create PricingSection wrapper and integrate all new sections into Index.tsx:

    1. **Create PricingSection.tsx:**
       - Simple wrapper component that imports and renders PricingPlans
       - Import PricingPlans from @/components/PricingPlans
       - Import motion from framer-motion
       - Structure:
         ```tsx
         const PricingSection = () => {
           return (
             <section className="py-24 px-6">
               <div className="max-w-7xl mx-auto">
                 <motion.h2
                   initial={{ opacity: 0, y: 20 }}
                   whileInView={{ opacity: 1, y: 0 }}
                   viewport={{ once: true }}
                   className="text-3xl md:text-5xl font-bold text-center mb-12 uppercase tracking-wide"
                 >
                   Choose Your Plan
                 </motion.h2>
                 <PricingPlans />
               </div>
             </section>
           );
         };
         export default PricingSection;
         ```
       - No need to modify PricingPlans.tsx—reuse as-is

    2. **Update Index.tsx:**
       - Add imports for new components:
         ```tsx
         import AboutSection from "@/components/AboutSection";
         import FAQSection from "@/components/FAQSection";
         import PricingSection from "@/components/PricingSection";
         ```
       - Replace placeholder divs in about, faq, pricing sections with actual components:
         ```tsx
         <section id="about" className="py-24">
           <AboutSection />
         </section>

         <section id="faq" className="py-24">
           <FAQSection />
         </section>

         <section id="pricing" className="py-24">
           <PricingSection />
         </section>
         ```
       - Remove placeholder div comments
       - Verify all sections render in correct order: hero, features, about, faq, pricing

    3. **Final checks:**
       - Ensure all section IDs still present (hero, features, about, faq, pricing)
       - Ensure proper spacing (py-24 on each section)
       - Verify no duplicate imports or components
  </action>
  <verify>
    1. Run `npm run dev`
    2. Scroll through entire page, verify all sections render:
       - Hero with gradient and CTA
       - Features with 4 categories
       - About with company story
       - FAQ with accordion
       - Pricing with 3 tiers and toggle
    3. Click header nav links, verify scroll to each section works
    4. Test "Start Trial" button, verify scrolls to pricing
    5. Test pricing toggle (monthly/yearly), verify prices update
    6. Check mobile: all sections stack properly, readable text
    7. Run `npm run build` and verify no TypeScript errors
  </verify>
  <done>
    PricingSection.tsx created as wrapper for existing PricingPlans component. Index.tsx updated with imports and renders for AboutSection, FAQSection, PricingSection. All 5 sections (hero, features, about, faq, pricing) render in one-page scroll layout with proper section IDs and spacing.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **About section:**
   - Company story renders with 2-3 paragraphs
   - Trust signals list displays (TPN+, SOC2, uptime)
   - Dark theme styling consistent with site
   - Framer Motion animations trigger once

2. **FAQ section:**
   - "Frequently Asked Questions" title visible
   - Accordion with 6-8 questions renders
   - Questions expand/collapse smoothly (single mode)
   - Dark theme with rounded corners and proper spacing

3. **Pricing section:**
   - "Choose Your Plan" title visible
   - Three pricing tiers render (Freelancer, Studio, Enterprise)
   - Monthly/yearly toggle works, prices update
   - Feature lists display for each tier
   - CTAs functional ("Select Plan", "Schedule a Call")

4. **Index.tsx integration:**
   - All 5 sections render in correct order
   - Navigation links scroll to correct sections
   - No console errors or warnings
   - Page loads without TypeScript errors

5. **Technical checks:**
   - `npm run build` succeeds
   - `npm run lint` passes
   - All sections responsive on mobile
   - No horizontal scroll at any viewport size
</verification>

<success_criteria>
About, FAQ, and Pricing sections complete when:
- User sees complete company story and trust signals in About section
- User can interact with FAQ accordion (expand/collapse questions)
- User sees all three pricing tiers with monthly/yearly toggle
- All sections styled consistently with dark minimal theme
- Navigation links scroll to all sections correctly
- Entire one-page layout renders without errors on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-foundation/01-03-SUMMARY.md` documenting AboutSection content, FAQ questions, PricingSection integration, Index.tsx updates, and any decisions made during implementation.
</output>
